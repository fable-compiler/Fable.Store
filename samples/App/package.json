{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "build": "dotnet fable src --run webpack",
    "start": "dotnet fable watch src --exclude Fable.SveltePlugins --run webpack serve --content-base public"
  },
  "devDependencies": {
    "svelte": "^3.31.0",
    "svelte-loader": "^2.13.6",
    "webpack": "^5.10.0",
    "webpack-cli": "^4.2.0",
    "webpack-dev-server": "^3.11.0"
  }
}
